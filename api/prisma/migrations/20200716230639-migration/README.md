# Migration `20200716230639-migration`

This migration has been generated by A. David Thyresson at 7/16/2020, 11:06:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"emailVerified" boolean   ,"id" SERIAL,"lastIp" text   ,"lastLogin" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"loginsCount" integer  NOT NULL DEFAULT 0,"name" text  NOT NULL ,"nickname" text  NOT NULL ,"picture" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."AccessToken" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"name" text  NOT NULL ,"revokedAt" timestamp(3)   ,"secret" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Site" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"name" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"url" text   ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."SiteToken" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"name" text  NOT NULL ,"revokedAt" timestamp(3)   ,"secret" text  NOT NULL ,"siteId" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Deploy" (
"branch" text   ,"buildEndedAt" timestamp(3)   ,"buildId" text   ,"buildStartedAt" timestamp(3)   ,"cachedCommitRef" text   ,"ci" boolean   ,"commitRef" text   ,"context" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"deployPrimeUrl" text   ,"deployUrl" text   ,"errorAt" timestamp(3)   ,"errorMessage" text   ,"errorName" text   ,"goVersion" text   ,"head" text   ,"id" text  NOT NULL ,"netlify" boolean   ,"netlifyImagesCdnDomain" text   ,"nodeVersion" text   ,"pullRequest" boolean   ,"repositoryUrl" text   ,"rubyVersion" text   ,"siteId" text  NOT NULL ,"status" text   ,"successAt" timestamp(3)   ,"updatedAt" timestamp(3)  NOT NULL ,"userId" integer  NOT NULL ,"yarnVersion" text   ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.userId" ON "public"."User"("userId")

ALTER TABLE "public"."AccessToken" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Site" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."SiteToken" ADD FOREIGN KEY ("siteId")REFERENCES "public"."Site"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Deploy" ADD FOREIGN KEY ("siteId")REFERENCES "public"."Site"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Deploy" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200716230639-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,95 @@
+datasource DS {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = env("BINARY_TARGET")
+}
+
+model User {
+  id            Int           @id @default(autoincrement())
+  createdAt     DateTime      @default(now())
+  updatedAt     DateTime      @updatedAt
+  email         String        @unique
+  emailVerified Boolean?
+  lastIp        String?
+  lastLogin     DateTime      @default(now())
+  loginsCount   Int           @default(0)
+  name          String
+  nickname      String
+  picture       String
+  userId        String        @unique
+  accessTokens  AccessToken[]
+  sites         Site[]
+  deploys       Deploy[]
+}
+
+model AccessToken {
+  id        String    @id
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  name      String
+  secret    String
+  revokedAt DateTime?
+  User      User      @relation(fields: [userId], references: [id])
+  userId    Int
+}
+
+model Site {
+  id         String      @id
+  createdAt  DateTime    @default(now())
+  updatedAt  DateTime    @updatedAt
+  name       String
+  url        String?
+  user       User        @relation(fields: [userId], references: [id])
+  userId     Int
+  deploys    Deploy[]
+  siteTokens SiteToken[]
+}
+
+model SiteToken {
+  id        String    @id
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  name      String
+  secret    String
+  revokedAt DateTime?
+  site      Site      @relation(fields: [siteId], references: [id])
+  siteId    String
+}
+
+model Deploy {
+  id                     String    @id
+  createdAt              DateTime  @default(now())
+  updatedAt              DateTime  @updatedAt
+  nodeVersion            String?
+  yarnVersion            String?
+  rubyVersion            String?
+  goVersion              String?
+  ci                     Boolean?
+  netlify                Boolean?
+  buildId                String?
+  context                String?
+  repositoryUrl          String?
+  branch                 String?
+  head                   String?
+  commitRef              String?
+  cachedCommitRef        String?
+  pullRequest            Boolean?
+  deployUrl              String?
+  deployPrimeUrl         String?
+  netlifyImagesCdnDomain String?
+  status                 String?
+  errorName              String?
+  errorMessage           String?
+  buildStartedAt         DateTime?
+  buildEndedAt           DateTime?
+  errorAt                DateTime?
+  successAt              DateTime?
+  site                   Site      @relation(fields: [siteId], references: [id])
+  siteId                 String
+  User                   User      @relation(fields: [userId], references: [id])
+  userId                 Int
+}
```


